@if (this.User.Identity.IsAuthenticated)
{
    <ul class="navbar-nav ml-auto">
        <li class="nav-item navbar-text">
            <span>Hello, @this.User.Identity.Name!</span>
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log out", "Logout", "Employees", null, new { @class = "nav-link" })
        </li>
    </ul>
}