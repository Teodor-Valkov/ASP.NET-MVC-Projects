@if (this.User.Identity.IsAuthenticated)
{
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            @Html.ActionLink("Accounts", "Index", "Accounts", null, new { @class = "nav-link" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Payments", "Index", "Payments", null, new { @class = "nav-link" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("New Payment", "MakePayment", "Payments", null, new { @class = "nav-link" })
        </li>
    </ul>
}

@if (this.User.Identity.IsAuthenticated)
{
    <ul class="navbar-nav ml-auto">
        <li class="nav-item navbar-text">
            <span>Hello, @this.User.Identity.Name!</span>
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log out", "Logout", "Users", null, new { @class = "nav-link" })
        </li>
    </ul>
}